<% link_to("back to list",{:action=>'index'},:class=> 'back-lin')%>
Are you sure you wana Delete thi Subject?

<%= @subject.name%>
<%= link_to("Delete Subject",{:action=>'destroy',:id=>@subject.id})%>

